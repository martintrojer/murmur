node_modules/
dist/
*.tsbuildinfo

# Editor and tool scratch files (reflow buffers, temp drafts).
.m-*

# Working documents, not shipped artifacts. Specs and plans stay local;
# ARCHITECTURE.md is what survives of them. Shipped how-tos and the README
# screenshot are exceptions below. Use `docs/*` (not `docs/`) so negations work.
docs/*
!docs/setup.md
!docs/dash.png
