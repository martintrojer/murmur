node_modules/
dist/
*.tsbuildinfo

# Editor and tool scratch files (reflow buffers, temp drafts).
.m-*

# Working documents, not shipped artifacts. The spec and the implementation plan
# were how the design got settled and executed; ARCHITECTURE.md is what survives
# of them. Kept on disk because the reasoning is still useful locally, untracked
# because a spec that stops matching the code is worse than no spec.
docs/
